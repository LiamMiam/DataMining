# DataMining
DataMining


sudo apt update
sudo apt install python3-pip -y


# Attention ca break les system packages, donc ca l'installe sur votre machine, si vous voulez pas faut faire un environment virtuelle genre venv mais c'est un peu chiant 
pip3 install pandas folium scikit-learn numpy --break-system-packages
